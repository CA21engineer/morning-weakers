import 'package:flutter/material.dart';

class InputParticipantInfoPage extends StatelessWidget {
  static const String pageName = 'Input participant info page';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
